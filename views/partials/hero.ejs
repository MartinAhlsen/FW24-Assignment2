<% if (locals.hero) { %>
  <!-- Sorry about the inline styling! -->
  <div class="hero-container" style="background-image: url('<%= locals.hero %>');">
    <% if (locals.heading) { %>
     <h2 class="heading"><%= heading %></h2>
     <hr class="lower-hr">
    <% } else { %>
    <h2><%= type %></h2>
    <% } %>
  </div>
  <% } %>